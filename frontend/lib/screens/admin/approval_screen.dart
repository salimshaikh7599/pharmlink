import 'package:flutter/material.dart';

class ApprovalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Replace with actual list and approval functionality.
    return Scaffold(
      appBar: AppBar(title: Text('Pharmacy Approval')),
      body: Center(
        child: Text(
          'List of pharmacies awaiting approval goes here.',
          style: TextStyle(fontSize: 16),
        ),
      ),
    );
  }
}
